/* TUAHORAV2 — config de producción (GitHub Pages).
   Este archivo se carga ANTES que el script de config en index.html.
   - TUAHORA_BASE: ruta base en GitHub Pages (debe terminar en /)
   - TUAHORA_API : URL pública del Scheduler en Railway (SIN / final)
   Ojo: TUAHORA_API es la URL pública, NUNCA el dominio interno .railway.internal. */
window.TUAHORA_BASE = '/TeTocaV2/';
window.TUAHORA_API = ''; /* TODO: poner https://<proyecto>.up.railway.app cuando exista */
